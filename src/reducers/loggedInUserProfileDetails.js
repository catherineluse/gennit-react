
const initialState = {
    loggedInUserProfileDetails: []
}

const loggedInUserProfileDetails = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default loggedInUserProfileDetails