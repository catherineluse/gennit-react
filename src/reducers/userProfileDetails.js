const initialState = {
    userProfileDetails: {}
}
const userProfileDetails = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default userProfileDetails